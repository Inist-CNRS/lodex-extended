append = dump
label = json

[use]
plugin = basics
plugin = lodex
plugin = storage

[JSONParse]
separator = *

[standardize]

[swing]
test = pick(['URI', 'uri']).pickBy(_.identity).isEmpty()
[swing/identify]
