append = dump
label = json-lodex

[use]
plugin = basics
plugin = lodex
plugin = storage

[JSONParse]
separator = data.*

[swing]
test = pick(['URI', 'uri']).pickBy(_.identity).isEmpty()
[swing/identify]
